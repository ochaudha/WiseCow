apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  labels:
    app: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80        # Service port (accessible within cluster)
      targetPort: 4499 # Container port
  type: ClusterIP
